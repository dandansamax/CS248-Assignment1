#include "Camera.h"

Camera::Camera(Vector3f position, Vector3f viewDirection, float width, float height, int nx, int ny)
{
    // TODO
}

std::pair<Vector3f, Vector3f> Camera::getViewRay(int i, int j)
{
    // TODO
    Vector3f e, d;
    return std::make_pair(e, d);
}